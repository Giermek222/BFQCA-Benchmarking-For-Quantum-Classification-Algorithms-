import React from "react";
import { render } from "@testing-library/react";
import { Provider } from "react-redux";
import { store } from "./store_setup/store";
import App from "./App";

test("renders learn react link", () => {
  expect(1 === 1);
});
